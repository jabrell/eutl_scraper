import os

__MYPATH = os.path.dirname(os.path.abspath(__file__)) # path to current file

DIR_PARSED = os.path.join(__MYPATH, "../data/parsed/")  # directory with parsed data (relative path to current file's location)